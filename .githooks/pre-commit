#!/usr/bin/env sh
# Activation, une fois par clone :  git config core.hooksPath .githooks
# Contournable par `git commit --no-verify` — c'est la CI qui rattrape.
exec "$(git rev-parse --show-toplevel)/scripts/verifier.sh"
