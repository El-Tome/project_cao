#!/usr/bin/env sh
# Enable once per clone:  git config core.hooksPath .githooks
# Bypassable with `git commit --no-verify` — the CI is what catches that.
exec "$(git rev-parse --show-toplevel)/scripts/verify.sh"
